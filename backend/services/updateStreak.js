export async function updateStreak(userId) {
  // call in updatestatus
  // check user last_activity_date
  // if today, return
  // if yesterday, streak += 1
  // if before yesterday, streak = 1
  // then update user last_activity_date
}
